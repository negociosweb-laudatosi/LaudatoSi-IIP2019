mixin crearArticulo()
    pre #{vardump(proyecto)}
    .formularioE
        form(action='#'  method="POST")
            legend Favor llenar todos los datos
            .contenedor-campos
                .campo
                    label(for='titulo') Nombre del Artículo
                    input(type='text' name='titulo' placeholder='Ingresa el nombre' id="titulo" )
                .campo
                    label Categoría del Artículo
                    select#Categoria-Articulo(name='CategoriaA')
                //-   sincronizar con la base de datos
                        option(value='1') Categoría 1
                        option(value='2') Categoría 2
                        option(value='3') Categoría 3
                .campo-edit
                    textarea#edit
                .enviar
                    input(type="submit" value="Guardar" class="button")
                    a.button(href='#') Cancelar



